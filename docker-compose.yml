version: "3.9"

services:
  rakrilabs-web:
    build: .
    container_name: rakrilabs-web
    restart: always
    expose:
      - "3000"
    env_file:
      - .env
    networks:
      - shared_network

networks:
  shared_network:
    external: true
